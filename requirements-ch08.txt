# Chương 8 – học sâu trên dữ liệu lớn (cần GPU). Cài sau requirements.txt, trên Python 3.11
# (TensorFlow 2.15.1 chỉ hỗ trợ Python 3.9 – 3.11).
mlflow==2.17.2
# MLflow 2.17.2 không chạy được backend CSDL (sqlite, PostgreSQL…) với SQLAlchemy 2.1 nên ghim dòng 2.0
sqlalchemy==2.0.54
tensorflow==2.15.1
torch==2.4.1
# Horovod đã lưu trữ (archived) từ 9/2026, 0.28.1 là phiên bản cuối; tài liệu chính thức chỉ ghi nhận
# tương thích tới TensorFlow 2.12 nên cần tự kiểm thử; khi cài cần trình biên dịch C++ và NCCL hoặc MPI.
horovod==0.28.1
petastorm==0.12.1
